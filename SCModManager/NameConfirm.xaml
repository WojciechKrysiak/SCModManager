<Window x:Class="SCModManager.NameConfirm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SCModManager"
        mc:Ignorable="d"
        Title="Confirm Name" 
        VerticalContentAlignment="Top"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="300">
    <StackPanel Orientation="Vertical" Margin="5">
        <TextBlock Margin="5">Enter a name for the selection:</TextBlock>
        <TextBox Text="{Binding Name}" Margin="5"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >

            <Button Margin="5" Width="50" Command="{Binding Ok}">OK</Button>
            <Button Margin="5" Width="50" Command="{Binding Cancel}">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>
